{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Abrir index.html direto",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}